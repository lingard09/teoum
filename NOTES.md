# teoum 작업 노트

## 현재 상태 (2026-09-13)
- Vite + React(JS) 뼈대만 생성. 데모 화면은 제거하고 빈 `App.jsx`만 남김.
- 라우터, 상태관리, 스타일 방식(CSS Modules / styled-components 등)은 아직 안 넣음.

## 미확정 — 피그마 디자인 받은 뒤 결정
- 페이지 목록 → 라우터(react-router) 필요 여부
- 화면 규격: 모바일 고정 프레임 vs 반응형
- 폰트, 컬러 토큰

## 명령어
- `npm run dev` 개발 서버
- `npm run build` 빌드
- `npm run lint` oxlint
