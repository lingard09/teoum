# 한국관광공사 TourAPI(KorService2) 서비스키. data.go.kr에서 발급받은 "Encoding" 형태의 키를
# 그대로 붙여넣는다(이미 URL 인코딩되어 있음 — 다시 인코딩하면 인증이 깨짐).
# 이 파일은 커밋해도 되지만, 실제 키가 들어가는 .env.local은 절대 커밋하지 않는다.
VITE_TOURAPI_SERVICE_KEY=
